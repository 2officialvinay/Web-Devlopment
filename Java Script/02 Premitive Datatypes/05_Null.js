// In JavaScript, null is a primitive value that represents the intentional
// absence of any object value. It is used to signify that a variable, object,
// or property does not have a value.

let range = null;
console.log(typeof(range));
console.log(range);